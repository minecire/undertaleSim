class object{
    constructor(x,y,dir,otp){
        this.x = x;
        this.y = y;
        this.direction = dir;
        this.objectType = otp;
    }
}

function doObject(object){
    if(object.type == "bone1"){ //first object type - a bone

    }
}